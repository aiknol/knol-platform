# =============================================================================
# Knol Docs Website — Cloudflare Pages Configuration
# Static site deployment for docs.aiknol.com (docs)
# =============================================================================

name = "knol-docs"
compatibility_date = "2024-09-25"
pages_build_output_dir = "../frontend/docs/.next-build"
