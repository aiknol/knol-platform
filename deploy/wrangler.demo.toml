# =============================================================================
# Knol Demo UI — Cloudflare Pages Configuration
# Static site deployment for demo.aiknol.com (demo)
# =============================================================================

name = "knol-demo"
compatibility_date = "2024-09-25"
pages_build_output_dir = "../frontend/demo/out"
